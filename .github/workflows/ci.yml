name: CI

on:
  push:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'LICENSE'
    tags:
      - 'v*'
  pull_request:
    branches: [main]
    paths-ignore:
      - '*.md'
      - 'docs/**'
      - 'LICENSE'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: cd app && npm ci
      - run: make test

  publish:
    needs: test
    if: github.event_name == 'push'
    runs-on: ubuntu-latest
    permissions:
      contents: write
      packages: write
    steps:
      - uses: actions/checkout@v4

      - uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Determine tags
        id: tags
        run: |
          SHA=$(echo "${{ github.sha }}" | cut -c1-7)
          TAGS="ghcr.io/wheelhouz/open-brain:sha-${SHA},ghcr.io/wheelhouz/open-brain:latest"
          CONFIG_TAGS="ghcr.io/wheelhouz/open-brain-config:sha-${SHA},ghcr.io/wheelhouz/open-brain-config:latest"
          if [[ "${{ github.ref }}" == refs/tags/v* ]]; then
            VERSION="${GITHUB_REF#refs/tags/v}"
            TAGS="${TAGS},ghcr.io/wheelhouz/open-brain:${VERSION}"
            CONFIG_TAGS="${CONFIG_TAGS},ghcr.io/wheelhouz/open-brain-config:${VERSION}"
            echo "version=${VERSION}" >> "$GITHUB_OUTPUT"
          fi
          echo "tags=${TAGS}" >> "$GITHUB_OUTPUT"
          echo "config_tags=${CONFIG_TAGS}" >> "$GITHUB_OUTPUT"

      - name: Build and push app image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          tags: ${{ steps.tags.outputs.tags }}

      - name: Build and push config image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile.config
          push: true
          tags: ${{ steps.tags.outputs.config_tags }}

      - name: Create GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        run: gh release create "v${{ steps.tags.outputs.version }}" --generate-notes
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
